<h1>Edit Car Status</h1>
<%= form_with model: [:admin, @car], local: true do |form| %>
  <div>
    <%= form.label :status %>
    <%= form.select :status, Car.statuses.keys %>
  </div>
  <div>
    <%= form.submit 'Update Status' %>
  </div>
<% end %>
<%= link_to 'Back', admin_cars_path %>
